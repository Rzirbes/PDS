export enum BodySide {
    LEFT = 'LEFT',
    RIGHT = 'RIGHT',
    BOTH = 'BOTH',
    NONE = 'NONE',
}

export enum InjuryContext {
    TRAINING = 'TRAINING',
    WARMUP = 'WARMUP',
    GAME = 'GAME',
    DAILY_ACTIVITY = 'DAILY_ACTIVITY',
    UNKNOWN = 'UNKNOWN',
}